# 更新日志

## v1.6.1 (2026-09-25)

### 修复
- **非网易云在线歌单里的歌放不出来（队列占位歌曲解析失败）**：`DualPlayerEngine` 解析 cloud://lx 占位时 `if (lxReady) 插件解析 else null ?: 内置兜底` 存在 Kotlin 优先级陷阱（else 分支吞掉 elvis），插件就绪但解析失败时内置官方接口兜底永远不会执行——搜索点击的歌因走 `resolvePlayableSong`（兜底写法正确）能播，而歌单整队入列 / 搜索排队的占位歌曲全部解析失败。现加括号对齐「插件优先、内置兜底」语义；同时占位 JSON 解析对空串 songmid/hash 回落到歌曲 id（`optString` 的默认值仅对缺失 key 生效，酷我歌单歌曲无 hash 时此前拿到空串）。

### 新增
- **播放器样式：Expressive（移植自 Rhythm）**：设置 → 外观 → 正在播放新增「播放器样式」，用 1:1 移植自 Rhythm 的 `ExpressiveButtonGroup` 分段胶囊按钮在「经典」与「Expressive」两套全屏播放器布局间切换（选中项变 primary 胶囊高亮 + 勾选图标，本地记住）。切换后按所选样式显示对应的专属设置分组：经典显示「播放器控制」组（控制按钮无色块）；Expressive 显示「Expressive 播放器」组——「强调色背景」（控制卡片/切换圆钮改用封面强调色玻璃，对应 Rhythm accentGlass）与「合并控件」（收藏/随机/循环/歌词/队列按钮并入控制卡片），两项均为真实生效功能。Expressive 布局 1:1 移植 Rhythm：封面随播放态弹簧缩放（播放 1.0 / 暂停 0.85）、超大 Black 字重歌名按队列方向横向滑入、圆角 32dp 控制卡片（宽播放钮 + 圆形上下曲钮 + 内嵌进度区）、底部功能胶囊，电台模式自动隐藏随机/循环/上下曲与进度，兼容扁平控制按钮与播放器主题取色；经典布局及其全部原有设置零改动。
- **Web 远控第四样式「Folia」**：1:1 复刻 folia 悬浮播放控制条——#09090b 深底 + Inter 字体，底部悬浮玻璃胶囊 Dock（backdrop-blur-3xl、rounded-full、shadow-2xl）：折叠态为细进度条胶囊（60% 宽，移动端 80%），悬停 20ms / 暂停时自动弹簧展开为完整控制布局（48px 圆形播放钮 + 居中歌名与 clipPath 圆角填充进度条 + 10px 等宽时间标签 + 循环 / 歌词按钮，stiffness 280 / damping 24 弹簧近似），鼠标离开 100ms 收起；中央区显示歌名 / 歌手 / 当前歌词字幕（歌词按钮开关字幕，无歌词时自动禁用）；播放 / 拖动进度接入真实控制链路，循环按钮为本地 UI 状态（远控协议暂无循环控制）。

### 改进
- **搜索结果里的歌单点击改为整单入队播放**：点击歌单卡片不再进入独立在线歌单详情页，改为拉取整单歌曲后以 cloud://lx 占位懒解析一次性入队播放，并直接打开软件已有的播放列表（队列）界面；独立在线歌单详情页与底部预览面板已移除。

### 构建
- 版本号 v1.6.0 (57) → v1.6.1 (58)。

## v1.6.0 (2026-09-25)

> 登录相关模块本次未做任何改动。

### 修复
- **在线（网易云）歌手头像加载不出来**：歌手主页信息走新版 `/artist/head/info/get` 接口，其头像字段为 `avatar` / `cover`，而解析代码只找 `picUrl`/`img1v1Url`/`avatarUrl`——字段对不上导致头像恒为空（请求"成功"但不触发回退）。现已补齐字段读取，网易云歌手主页头像恢复正常。
- **网易云评论「查看回复」入口重构**：入口固定显示在「回复」按钮右侧（对齐网易云官方布局），不再依赖回复数判断有无；展开后原地变为「收起回复」，回复列表仍在下方展开。
- **打开网易云评论概率性崩溃（LazyColumn 重复 key）**：服务端分页偶发返回重复评论，`c_{id}` key 冲突直接崩溃。现首屏/加载更多/本地插入全路径按 commentId 去重。
- **B 站评论 -400 加固**：评论主列表/楼中楼/回退链全部请求对齐 PiliPlus 指纹头（`env: prod`、`app-key: android64`、`x-bili-aurora-zone: sh001`），配合四层自愈链（登录态传统接口 → 带登录 cookie 游标 → 匿名 buvid → 无 Cookie web 配方）最大化加载成功率。
- **网页远控切歌后串流断开**：切歌瞬间「按服务器位置回跳」会用旧曲目时长对新 src 执行 currentTime 赋值，中断新流加载导致串流静默断掉。现增加 3 秒切歌保护窗 + 元数据就绪/时长吻合校验后才回跳位置；流加载失败自动重载（最多 2 次）自愈。
- **设置 → 在线音源 进入/退出动画与其它设置子页不一致**：该页被误归入主 Tab 路由获得定向滑动过渡。现归位为普通设置子页，统一使用 AOSP SharedAxis 过渡。
- **可折叠顶栏（毛玻璃样式）副标题把胶囊顶得上移**：毛玻璃样式下隐藏副标题，主标题垂直居中，胶囊位置稳定。
- **设置 Tab 选中时图标不变填充样式**：底栏/悬浮底栏/平板侧栏的设置图标此前选中与未选中共用同一 outline 资源。现新增 Material Symbols 填充版齿轮图标，选中时三种导航形态下均正确切换为填充样式。
- **淡入淡出 + 单曲/列表循环时，淡入到下一曲后歌曲暂停**：交叉淡入淡出进行中，旧曲到达曲尾时 `pauseAtEndOfMediaItems` 会自动置 `playWhenReady=false`（reason=END_OF_MEDIA_ITEM），引擎的「用户暂停同步」逻辑把它误判为用户暂停，把正在淡入的下一曲一并暂停——fade 结束换轨后新曲就停在暂停态。现暂停同步排除曲尾自动暂停（仅同步真正的用户暂停：手动/蓝牙断开/焦点丢失），下一曲淡入后正常继续播放。
- **文件管理器拉起播放时 mini player 进度条卡死**：进度轮询的「UI 歌曲与播放器媒体项错位保护」在错位无法被重同步纠正时（外部拉起播放的歌曲不在媒体库中）会无限跳过位置采样，进度条永久冻结。现错位持续超过重同步窗口且重同步无效时，放弃阻塞并按播放器实际位置继续采样，进度条不再卡死。
- **悬浮底栏下 mini player 圆角变小**：悬浮导航栏样式下 mini player 的顶部/底部圆角被硬编码为 28dp，未使用用户设置的导航栏圆角。现与其他底栏样式一致，跟随「导航栏圆角」设置。
- **悬浮底栏不遵守「禁用模糊」设置**：悬浮导航栏胶囊与搜索圆钮的磨砂效果此前无条件启用，无视「导航栏模糊」与「禁用所有模糊」开关。现已接入两个设置：关闭模糊时以不透明 surface 底色渲染。
- **歌曲播放一半退出后再播放失败**：解析直链缓存（占位 URI → 真实直链）此前无有效期——重播歌曲时无条件命中旧直链，直链过期（网易云/酷我等通常 10-30 分钟失效）后返回 403/410 且部分错误恢复路径不清缓存，导致反复播放失败。现缓存条目带 15 分钟 TTL（与各流媒体代理内部 TTL 对齐），过期自动作废并强制走完整解析链拿新鲜直链。
- **B 站评论区显示 `(-400) 请求错误`**：登录态走传统分页接口 `/x/v2/reply`，在 TV 登录 cookie 下会被 B 站以 -400 拒绝，而此前的降级链不覆盖 -400，评论区直接报错。现改为四层自愈链：登录态传统接口 → 带登录 cookie 的 `/x/v2/reply/main` 游标（保留服务端点赞态）→ 匿名 buvid 会话 → 无 Cookie web 配方（PiliPlus 同款），并按偏移格式自动识别分页语义（页码 vs 游标），避免降级后"加载更多"重复第一页。
- **网易云评论有回复却不显示「查看回复」入口**：解析器只读 `ciCount` / `replyCount`，而官方评论接口的回复数字段是 `rcount`，导致回复数恒为 0。现三个字段兼容解析。
- **网易云评论点赞红心"过一会儿自己消失"**：点赞服务端调用失败（未登录/会话失效）时原先静默回滚，红心在网络请求返回后（数秒后）悄悄变灰，用户感知为"滚动回来就丢了"。现失败时明确弹出提示；发评后的列表刷新也不再丢弃本地点赞覆盖表。
- **网易云发评论后看不到自己刚发的评论**：官方索引存在延迟，发评后的整页刷新常拉不到新评论。现改为发评成功后本地即时插入评论列表（含当前用户昵称/头像），自动滚动定位并以主题色脉冲高亮两次提示"已发出"。
- **媒体库播放时上下曲偶发无反应**：媒体库点歌采用「单曲秒播 + 后台分段回填全库队列」，回填失败/被跳过时播放器时间线可能仍只有 1 首，而 UI 队列已显示多首，`seekToNext/Previous` 因「无下一/上一窗口」被 Media3 静默忽略。现回填失败时不再把完整队列同步到 UI（避免假象），并在 next/previous 检测到「播放器时间线 ≤1 首而 UI 队列 >1 首」的失步时自动以 UI 队列重建播放器并跳到目标曲。
- **在线搜索点歌后播放列表不稳定**：此前「点击歌曲 playUrl（会重置队列）」与「其余搜索结果逐首 enqueue」两条协程并发执行，先后顺序不定导致队列时而只剩单曲、时而丢失部分结果。现改为原子建队：点击歌曲解析真实直链后，连同其余结果（cloud://lx 占位懒解析）一次性传入 playSongs 构建完整队列。
- **在线歌单保存到本地后部分平台无法播放（解析失败）**：内置平台（QQ/酷狗/酷我/咪咕）拉取的歌单歌曲未统一标记音源，落库时缺失 source 的歌曲被兜底为网易云 —— 非网易云平台的数字 id 被存成 `netease://{假id}`，播放时永远解析失败。现拉取歌单时按歌单自身来源补齐 source，保存时也以歌单来源兜底。
- **部分在线歌曲播放报 `HTTP 410 Gone`（ExoPlayer errorCode 2004）**：在线直链解析顺序改为「用户导入的 JS 音源插件优先、Kotlin 内置源兜底」。此前内置源排在插件前，插件形同虚设，而内置第三方聚合（oiapi）返回的酷我签名直链部分已失效，导致播放失败。
- **用户导入的音源插件每次启动被删除**：`LxFileStore` 的启动清理逻辑会按文件名删除 `(推荐)全豆要-聚合音源 v4.1.js`、`全豆要-聚合音源 v9.3 …js`、`lx音源快速.js`，与用户自己导入的文件同名，每次启动会把插件删掉并回落到内置源；已移除该清理逻辑。
- **搜索页直接播放会播报「酷我音乐已为您开启免费听歌权限」**：搜索页与媒体库是两条播放链路，搜索页对 QQ音乐/酷狗/咪咕/酷我直接走内置源（酷我官方直链，部分歌曲试听流会插播该语音），而媒体库/收藏后播放走插件优先链路所以正常。现将搜索页播放对齐为「插件优先、内置兜底」，并在解析前等待 JS 引擎就绪。
- **未安装音源插件时播放解析被拖慢**：`LxJsEngine.awaitReady` 增加「未导入任何脚本立即返回」，避免每个音质档位白等 15 秒（整条音质链卡数十秒）。
- **保存的在线歌单一刷新歌曲就没了**：同步 / 清理 / 全量重建歌曲时会误删用户主动保存的云歌曲（`parent_directory_path = 'cloud'`，不属于 MediaStore 扫描范围）。现在这些歌曲被统一排除在删除之外，全量重建前会快照歌曲及其专辑 / 艺术家 / 关联表并在重建后完整回填。
- **咪咕、酷我平台标签混淆**：咪咕歌曲显示为「酷我」，现已修正为「咪咕」，并补充「酷我」标签。
- **B 站评论区无法加载**：`/x/v2/reply/main` 与楼中楼均为 web 接口，此前误用 HD 客户端 UA 被风控返回 `{"code":-352}`，改用浏览器 UA 后恢复。
- **B 站评论点赞 / 点踩状态不准确**：改为以服务端 `reply_control.action` 为准（1=已赞 2=已踩），并以 `like_state` 兜底。
- **网易云评论点赞状态重启后丢失**：本地新增点赞覆盖表并持久化，服务端写入成功后再落盘，失败自动回滚。
- **网易云的回复只能看到 10 条**：楼中楼回复支持时间游标分页，可「加载更多」。
- **倍速显示出现浮点长尾**（如 `1.1000000238x`）：四舍五入到 2 位小数并去掉多余尾零。

### 新增
- **Web 远控三样式 = 三套完整界面**：顶栏切换不再只是配色——「仪表」1:1 复刻 Reactor 整站布局（页头品牌区 / 堆芯舞台 / 控制台侧栏「音频输入·控制棒响应·实时读数」/ 页脚，播放/进度/切歌/刷新接入真实控制链路，灵敏度与平滑度滑杆实时可调，读数面板含电平/峰值频率/48 段频谱）；「玻璃」1:1 复刻 MineRadio 桌面（封面模糊壁纸 + 粒子海背景、右上毛玻璃按钮、底部玻璃播放条：封面缩略图+歌名歌手+红心+三键+点击跳转进度条）；「默认」保持原 Material 播放器。特效随样式自动接管，歌名/封面/进度/播放态三视图实时同步。
- **Web 远控音频可视化特效（1:1 复刻）**：网页遥控新增特效层，顶栏「特效」开关三档切换——
  - **堆芯**：1:1 移植 [Reactor Audio Visualizer](https://github.com/) 的切尔诺贝利控制棒阵列（MIT）——709 根控制棒、频段→半径映射（中心低频→边缘高频）、伪 3D 画家算法渲染、警戒条纹环，支持空背景拖拽旋转与滚轮缩放，暂停时进入演示波动；
  - **粒子**：1:1 移植 MineRadio 的封面粒子系统 SILK 形态（含完整 GLSL 着色器：simplex 噪声丝绸涟漪、封面色采样、指针斥力、点击涟漪数据纹理、辉光层、背景星河 1400 星）——专辑封面实时采样为 118×118 粒子海，随音乐低/中/高频与节拍律动；
  - 信号源为远控页流媒体本身（WebAudio AnalyserNode），特效层在 UI 之下、顶栏磨砂悬浮，选择本地记住；AudioContext 延迟至首次用户手势创建，避免自动激活时音频静音。
- **Web 远控双主题**：网页遥控界面新增两套可切换风格——「玻璃」（模仿 MineRadio：深空底色 + 毛玻璃面板 + 辉光强调色）与「仪表」（模仿 Reactor Audio Visualizer：扁平深橄榄绿 + 荧光黄绿 + 等宽字体 + 细边框仪器风），顶栏一键切换并本地记住选择，默认样式不变。
- **控制按钮无色块样式**：设置 → Now Playing 新增开关，开启后播放器的上一曲 / 播放暂停 / 下一曲渲染为纯图标（不带圆形色块背景，图标颜色随播放器主题），电台播放场景的播放按钮同样生效；默认关闭。
- **在线歌单搜索**（对齐落雪 `musicSdk.*.songList.search`）：网易云、QQ音乐、酷狗、酷我、咪咕全部可用，支持分页与「加载更多」。
- **在线歌单保存到本地**：一键把在线歌单歌曲写入媒体库并创建本地歌单，刷新不再丢歌。
- **内置在线音源**：酷我、QQ音乐、酷狗、咪咕的搜索 / 歌单 / 直链全部由 Kotlin 原生实现，无需导入任何 JS 插件即可使用。
- 搜索页新增「本地 / 在线」单排范围切换卡片，本地与在线各自记住上次的筛选选择。

### 改进
- **首页「像素卫士」按钮改为图标按钮**：与顶栏其它按钮样式一致（40dp 圆形、surfaceContainerHigh 底、纯色图标），移除胶囊文字与剩余分钟显示；听力进度背景填充与状态色保留。
- **可折叠顶栏重设计（18 个界面生效）**：未开启「禁用模糊」时，收起的顶部标题栏背景从纯色遮罩改为与首页一致的渐进模糊遮罩（HazeMaterials.regular + 垂直渐变模糊），收起的标题套入胶囊容器（底色与返回按钮一致、与返回按钮保持间距），随收起行程平滑淡入；开启「禁用模糊」则保持原有纯色样式（无胶囊）。设置/专辑/歌手/歌单分类/账号/均衡器/统计等全部使用共享 CollapsibleCommonTopBar 的界面统一生效。
- **mini player 位移弹簧动画**：进入隐藏底栏的页面（歌单/专辑/歌手详情等）时 mini player 随导航栏收起下移、返回时上移，此前是瞬间跳变；现以带过冲的弹簧过渡（MediumBouncy），移动手感更顺滑。
- **登录页过渡对齐「首页卡片 → 详情页」基准**：网易云 / Telegram / B 站三个登录页原先使用系统默认 Activity 过渡且常驻 BackHandler 吞掉手势，与导航层不一致。现通过窗口动画主题应用与导航层 AOSP SharedAxis 同款的进入/返回过渡（slide 1/3 + fade，350ms），并改为仅在有页内历史/步骤可回退时才拦截返回键——其余状态放行系统预测返回（API 34+ 手势跟手）。网易云登录页顶栏返回按钮的退出确认对话框保留。
- **冷启动提速约 2 秒**：移除独立的 SplashActivity（其内部固定等待 1200ms 文字动画 + 再叠加 MainActivity 内置 splash 固定 500ms + 400ms 渐隐，主内容开始组合前存在约 2.1 秒纯人工延迟）。现由 MainActivity 直接作为启动器，接入系统 SplashScreen API：启动图标显示至「偏好加载完成 + 主内容组合完成」的真实就绪条件，启动期间的重组 / 布局 / 首帧绘制成本全部被启动画面遮盖；删除了两个假 splash 界面与全部固定延时。顺带修复全新安装时偏好未加载就可能误触权限请求的竞态。
- **在线搜索改为「提交式」**：仅回车或点击搜索按钮才发请求，避免边输入边请求触发接口风控；本地搜索仍保留输入即搜（300ms 防抖）。
- **在线子分类**：网易云为「歌曲 / 歌手 / 歌单」，其余在线源为「歌曲 / 歌单」，切换音源时自动归一化。
- **QQ 音乐直链音质探测**：一次 vkey 请求探测完整候选链（F000 / Hires / SQ / M800 / M500 / C400），取第一个真实有权限的最高档；补全 purl 归一化、相对路径与 media_mid 还原，并新增请求节流防风控。
- **QQ 音乐播放与下载使用用户设置的首选音质**，无对应权限时按档位向下回退。
- **QQ 音乐解析补三级链路**：官方代理 + vkey → 落雪 tx 源 → 内置源（按「歌名 + 歌手」反查）。
- **内置源兜底链重写**：改用酷我官方 `mobi.s / convert_url_with_sign` 直链，新增 `Range: bytes=0-1` 可播校验与音质降级链（2000flac → 320kmp3 → 128kmp3），移除已失效的 `oiapi.net` 依赖。
- **网易云写操作全部改走 App 自带 weapi 客户端**（发评论 / 删评论 / 点赞 / 回复 / 楼中楼），路径对齐 NeteaseCloudMusicApi；新增登录态补齐逻辑，不再出现「页面有登录态、写操作却提示未登录」。
- **网易云评论发送 / 删除 / 回复由 eapi 改回 weapi**（对齐原版 comment.js），并修正专辑 / 歌单 / 电台 / 动态 / 视频的加密前缀。
- **B 站评论区对齐 PiliPlus**：大会员昵称粉色调、IP 属地显示、楼中楼对话标识。
- **评论区细节**：没有回复的评论不再显示「查看回复」入口，避免死交互；收起按钮与评论的间距压缩。

### 构建
- 版本号 v1.5.9 (56) → v1.6.0 (57)。

## v1.5.7 (2026-09-11)

### 新增
- Auto EQ 耳机预设推荐专区：热门机型（Sony WH-1000XM / AirPods Pro / Bose QuietComfort / Sennheiser HD600 等）一键应用优化均衡。
- 设置页新增推荐栏轮播，根据当前设置状态智能推荐（外观、播放、均衡器、歌词、AI 助手、媒体库、备份恢复等）。
- AI 输入框全新设计：聚焦时蓝→紫→红渐变流光旋转边框、自适应增高、物理键盘 Enter 发送 / Shift+Enter 换行。

### 改进
- AI 思考块显示真实推理过程：支持 Gemini thought 与 OpenAI 兼容 reasoning_content（如 DeepSeek），思考与正文分离流式输出，普通模型自动降级。
- Auto EQ 界面全面重构（模仿 Rhythm 设计）：预设卡片带品牌/类型与激活标记、筛选芯片选中动画、类型规范化（Over-Ear / In-Ear / Earbud）。
- 应用 Auto EQ 后调音器同步显示当前预设名称与频段曲线，清除后回到 FLAT。
- 远程封面取色稳定性提升：先尝试原样 URL，失败自动回退 https 版本。
- 播放器竖屏布局优化：消除长屏下控制按钮与进度条之间的大段空白。
- 悬浮导航栏搜索圆高度对齐导航栏。
- 像素卫士胶囊进度填充两端圆角修正。
- 蓝牙预设切换通知、Toast 提示全面接入多语言资源。

### 构建
- Release 包新增 x86 ABI 支持（arm64-v8a + x86 通用包），x86 模拟器/设备可直接安装。
