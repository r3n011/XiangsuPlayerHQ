# 更新日志

## 2026-09-05

### 新增
- 播放器「绚丽背景」与歌词「绚丽背景」拆分为两个独立开关，均默认启用；未设置自定义播放器背景时，播放器背景由「播放器绚丽背景」单独控制，不再与歌词背景联动
- 设置页外观分类新增「播放器绚丽背景」开关选项，并已纳入设置搜索索引
- AI 助手改为一问一答流式输出：逐字实时显示回答并带光标，工具调用步骤仅记录在思考块
- AI 聊天记录本地持久化：通过 DataStore 以 JSON 存储，启动时恢复历史、对话结束自动保存、清空按钮同步清除记录
- AI 助手新增「解读当前播放歌曲」能力（`get_current_song`）与「联网搜索」能力（`web_search`，解析 Bing 返回前 4 条）
- 播放器新增倍速按钮显示开关（`showPlaybackSpeedButton`）
- 发现按钮改为多选逻辑：漫游 / 电台 / AI 助手可独立勾选，勾选 1 个时按钮显示对应图标并直接进入，勾选 ≥2 个时点击弹出功能菜单，1 个都不勾选时隐藏按钮
- AI 生成歌单中的歌曲支持直接点击播放（点击后写入媒体库再播放）

### 修复
- 修复平板端播放器旋转屏幕后布局锁死的问题：方向判定由 `LocalWindowInfo.containerSize` 改为 `LocalConfiguration`（优先 `orientation`，兼容分屏窄窗口）
- 修复软件每次打开都卡顿的问题：媒体库全量加载（歌曲 / 专辑 / 歌手 / 文件夹）从冷启动立即并发执行改为延迟到首帧渲染后再执行，降低启动 CPU/IO 峰值
- 修复搜索页面在线歌曲缓存所有封面、导致本地缓存暴涨的问题
- 修复媒体库查询崩溃 "Couldn't read row … from CursorWindow"：`getAllSongs` 由 `SELECT *` 改为 `SONG_LIST_PROJECTION` 投影列，排除歌词等大字段
- 修复 AI 助手所有回答均显示「没有找到适合的答案」：流式输出不再吞掉真实错误，并在流式失败或返回为空时追加非流式兜底重试
- 修复平板模式本地歌手页布局问题（固定手机端布局路径）
- 修复手机模式在线歌手点击头像 / 背景图无法查看大图的问题（遮罩点击改为可开大图）
- 修复整个软件图片有概率随机空白、疑似内存管理问题：封面软件位图解码上限由 `2048×2048` 降至 `1600×1600`，每张封面 Java 堆内存占用削减约 40%，减轻内存缓存被大封面挤爆后的整页图片空白
- 修复 `Icons.Rounded.Album` 不在默认图标集导致的编译失败，改用已有的 `MusicNote`

### 变更
- 播放器背景复刻歌词背景的模糊氛围效果（Apple Music 风格旋转封面），由「播放器绚丽背景」/动态背景开关控制
- 歌词「绚丽背景」描述同步更新：仅作用于歌词界面，不再兼任播放器背景
- 自定义播放器背景优先级说明文案更新：自定义图片背景 > 动态封面背景
- 完善设置搜索索引：补齐倍速按钮、自定义播放器背景、歌词动态背景、歌词渐变遮罩等缺失项，并新增「倍速/速度」「背景图/壁纸」「动态背景/氛围」等搜索关键词

## 2026-09-04

### 新增
- 「发现」按钮弹出菜单新增「AI 助手」入口；漫游 / 电台 / AI 助手三项可在设置中独立开关显示
- AI 助手重写为可自主执行操作的主控助手：支持生成歌单、查找设置、在线搜索等，由 AI 决定要执行的操作；进入 AI 助手页自动收起底部导航栏与 mini player，避免输入框被遮挡；点击建议卡片直接发送
- 更新弹窗新增「后台更新」选项：前台服务在通知栏实时显示下载进度，下载完成后自动拉起安装
- 应用启动时自动检查更新（每天仅检查一次）

### 修复
- 平板模式歌手页左侧背景图缺少向下渐变黑的问题
- 手机模式点击歌手头像或背景图无法查看大图的问题
- 悬浮导航栏模式下下拉 mini player 会卡住、无法自动归位的问题
- 悬浮导航栏与 mini player、屏幕下侧间距过大：胶囊贴底对齐、mini player 紧贴悬浮栏
- 悬浮导航栏选中指示容器上下 / 左右间距不对称的问题（统一为等距）

### 变更
- 在线搜索结果默认不再写入媒体库：仅真正点播开始播放的歌曲才入库，避免媒体库 / 用户数据无限膨胀

## 2026-08-16

### 新增
- 歌词界面新增自定义字体：支持导入 `.ttf` / `.otf` 字体，字体选项采用流式布局（每行最多 3 个），长按自定义字体可删除
- 新增 Apple Music 风格旋转封面背景：封面四块旋转 + RenderEffect 模糊，低版本回退为模糊封面
- 新增歌词实心遮罩透明度设置（`lyricsSolidOverlayAlpha`）
- 新增在线音源音质动态目录（`MusicQualityCatalog`）：识别音源脚本注册的 `qualitys`，动态生成音质选项与向下兼容降级链，替代硬编码档位（含网易云 VIP 音质 `jymaster` / `sky` / `jyeffect` 等）

### 修复
- 修复导入自定义字体后，歌词设置界面字体列表不立即刷新、需重新开关设置界面才显示的问题

### 变更
- 歌词动画升级为 Apple Music 风格：低刚度弹簧（缩放/颜色/位移）、行间距差异化物理弹力、非当前行按距离渐强模糊
- 云音乐设置界面重构

## [未发布] - 1.4.8

### 新增
- 歌词界面新增「歌词显示」三态选择器：**仅翻译 / 仅原文 / 原文+翻译**（胶囊按钮样式，与歌词字号选择器一致），替代原来的「显示原文」开关
- 同步歌词、词级高亮歌词、纯文本歌词三条渲染路径均支持三态显示；「仅翻译」模式下翻译作为主文本展示，无翻译时自动回退原文
- 排行榜平台选择器改为与媒体库一致的紧凑大胶囊（标题段 + 中间 4dp 间隙 + 箭头段），仅选中平台时展开箭头段，箭头在榜单切换器打开时旋转向上并展开圆角
- 歌曲评论面板（CommentSheet）加入与歌词页面一致的预测性返回动画（缩放 92% + 下滑 8%，API 34 手势逐帧驱动）
- 字体设置选择器统一为歌词字号选择器样式（48dp 高、50dp 选中圆角胶囊）

### 修复
- **在线歌词「原文/翻译」顺序颠倒**：网易云歌词合并逻辑不再按"中文字符多的一方作主文本"（此前外文歌曲会把中文翻译当成主文本），一律以原文为主、翻译为次；并丢弃旧版写入的反向歌词缓存（JSON 缓存版本化 + 在线歌曲跳过数据库缓存）
- **酷我 / 咪咕榜单内容错位**：切换平台时清空旧榜单数据、丢弃过期响应，修复"咪咕里显示酷我、酷我只剩一个云"的问题；酷我歌词/榜单解密兼容 base64 中未编码的 `+` 字符
- **低性能设备体验优化**：修复打开 mini player 内容闪动两次、切歌时跳回上一曲又跳到下一曲的问题；优化媒体库播放前 3 秒卡顿
- 修复搜索页编译错误（缺少右括号导致 `Expecting ')'`）

### 变更
- **删除酷我、咪咕排行榜**：排行榜平台仅保留 网易云 / QQ音乐 / 酷狗
- 排行榜平台胶囊字体改为常规字重、字号缩小（16sp），胶囊高度 44dp
- 手表端歌词同步适配新的三态显示模式
